services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
      args:
        USER_ID: ${UID:-1000}
        GROUP_ID: ${GID:-1000}
    volumes:
      - ./workspace:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
      - ./claude:/home/claude
